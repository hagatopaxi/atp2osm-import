<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" xmlns:xhtml="http://www.w3.org/1999/xhtml">
{%- for endpoint, label, desc in pages %}
{%- set path = url_for(endpoint) %}
{%- for code in locales %}
  <url>
    <loc>{{ root }}/{{ code }}{{ path }}</loc>
{%- for alternate in locales %}
    <xhtml:link rel="alternate" hreflang="{{ alternate }}" href="{{ root }}/{{ alternate }}{{ path }}" />
{%- endfor %}
    <xhtml:link rel="alternate" hreflang="x-default" href="{{ root }}{{ path }}" />
  </url>
{%- endfor %}
{%- endfor %}
</urlset>
